/**
 * Options for mounting a style module.
 *
 * @public
 * @unofficial
 */
export interface StyleModuleMountOptions {
  /** A Content Security Policy nonce for the generated style elements. */
  nonce?: string;
}

/**
 * Controls tree iteration behavior.
 *
 * @public
 * @unofficial
 */
export declare const enum IterMode {
  ExcludeBuffers = 1,
  IgnoreMounts = 4,
  IgnoreOverlays = 8,
  IncludeAnonymous = 2
}

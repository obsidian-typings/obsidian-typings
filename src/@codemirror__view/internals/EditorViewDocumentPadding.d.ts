/**
 * Padding above and below the document.
 *
 * @public
 * @unofficial
 */
export interface EditorViewDocumentPadding {
  /** The bottom padding in pixels. */
  bottom: number;
  /** The top padding in pixels. */
  top: number;
}

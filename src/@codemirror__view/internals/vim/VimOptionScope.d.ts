/**
 * Whether an option is read from or written to the editor it was addressed with, or the global
 * defaults every editor falls back to.
 *
 * @public
 * @unofficial
 */
export type VimOptionScope = 'global' | 'local';

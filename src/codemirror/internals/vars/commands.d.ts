import type { Cm5Editor } from '../Cm5Editor.d.ts';

/**
 * A map of built-in CodeMirror 5 commands.
 *
 * @public
 * @unofficial
 */
export declare const commands: Record<string, (cm: Cm5Editor) => void>;

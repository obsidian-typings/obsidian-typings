/**
 * Options for downloading a URL in a webview.
 *
 * @public
 * @unofficial
 */
export interface ElectronWebviewTagDownloadURLOptions {
  /** Custom headers for the download request. */
  headers?: Record<string, string>;
}

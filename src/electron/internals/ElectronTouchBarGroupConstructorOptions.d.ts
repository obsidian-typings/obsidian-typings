import type { ElectronTouchBar } from './ElectronTouchBar.d.ts';

/**
 * Options for creating a {@link ElectronTouchBarGroup}.
 *
 * @public
 * @unofficial
 */
export interface ElectronTouchBarGroupConstructorOptions {
  /** Items to display as a group. */
  items: ElectronTouchBar;
}

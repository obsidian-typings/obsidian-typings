/**
 * Electron session for managing browser sessions, cookies, and cache.
 *
 * @public
 * @unofficial
 */
export interface Session {
  /** The default session instance. */
  defaultSession: Session;
}

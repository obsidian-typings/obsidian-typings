/**
 * Default renderer options.
 *
 * @remark Only the options Obsidian itself changes are described here.
 *
 * @public
 * @unofficial
 */
export interface PixiRenderOptions {
  /** Whether the renderer logs its version and rendering backend to the console on creation. */
  hello: boolean;
}
